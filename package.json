{
  "name": "obsidian-table-drag",
  "version": "0.1.0",
  "private": true,
  "description": "Resizable Markdown table columns (and optional rows) for Obsidian.",
  "author": "Daniel + Agent Mode",
  "license": "MIT",
  "scripts": {
    "build": "node ./scripts/build.cjs",
    "dev": "node ./scripts/build.cjs --watch",
    "clean": "node -e \"try{require('fs').rmSync('main.js',{force:true});require('fs').rmSync('build',{force:true,recursive:true});}catch(e){}\""
  },
  "devDependencies": {
    "@codemirror/view": "^6.24.0",
    "@types/node": "^20.14.2",
    "esbuild": "^0.21.5",
    "obsidian": "^1.6.0",
    "typescript": "^5.5.4"
  }
}
